# Property Regression Files

`make test-prop` persists minimized `proptest` failures here.

If `properties.txt` appears or changes, keep it with the test fix that explains
the generated case. The file lets future runs replay the minimized input before
trying fresh generated cases.
