[package]
name = "gtk-lush-viewport"
version = "0.0.0"
edition = "2024"
rust-version.workspace = true
license = "MIT OR Apache-2.0"
description = "Scrollable viewport observation helpers for gtk-rs"
readme = "README.md"
repository = "https://github.com/cominotti/lushtext"
keywords = ["gtk", "gtk4", "viewport", "rust"]
categories = ["gui", "development-tools"]
include = [
    "Cargo.toml",
    "CHANGELOG.md",
    "LICENSE-APACHE",
    "LICENSE-MIT",
    "README.md",
    "examples/**",
    "src/**",
]

[package.metadata.lushtext-agent]
performance-roots = ["src"]

[lints]
workspace = true

[dependencies]
glib = { workspace = true }
gtk4 = { workspace = true }
