[package]
name = "gtk-lush-signals"
version = "0.0.0"
edition = "2024"
rust-version.workspace = true
license = "MIT OR Apache-2.0"
description = "RAII lifetime helpers for gtk-rs signals and bindings"
readme = "README.md"
repository = "https://github.com/cominotti/lushtext"
keywords = ["gtk", "gtk4", "signals", "rust"]
categories = ["gui", "development-tools"]
include = [
    "Cargo.toml",
    "CHANGELOG.md",
    "LICENSE-APACHE",
    "LICENSE-MIT",
    "README.md",
    "examples/**",
    "src/**",
]

[package.metadata.lushtext-agent]
performance-roots = ["src"]

[lints]
workspace = true

[dependencies]
glib = { workspace = true }

[dev-dependencies]
gio = { workspace = true }
gtk4 = { workspace = true }
