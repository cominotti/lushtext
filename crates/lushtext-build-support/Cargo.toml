[package]
name = "lushtext-build-support"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
license = "GPL-3.0-or-later"
publish = false

[lints]
workspace = true

[dependencies]
workspace-hack = { version = "0.1", path = "../../workspace-hack" }
