interface:
  display_name: "Rust Hot-Path Review"
  short_description: "Deep-review Rust hot paths and acceleration"
  default_prompt: "Use $gtk-perf-rust-optimize for an explicit deep dive into Rust hot-path correctness and established acceleration patterns."
policy:
  allow_implicit_invocation: false
