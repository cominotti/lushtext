interface:
  display_name: "GTK Responsiveness Review"
  short_description: "Deep-review GTK main-loop and async behavior"
  default_prompt: "Use $gtk-responsiveness for an explicit deep dive into GTK main-loop, async, signal, timer, and lifetime behavior."
policy:
  allow_implicit_invocation: false
