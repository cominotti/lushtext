interface:
  display_name: "GTK Agentic Debugging"
  short_description: "Debug live GTK sessions with reproducible evidence"
  default_prompt: "Use $gtk-agentic-debugging to run the app, capture runtime evidence, and separate live GTK symptoms from toolkit-contract issues."
