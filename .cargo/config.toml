# Compilation speed optimizations for local development.
#
# Linker overrides are set via Makefile (RUSTFLAGS) rather than here,
# so builds don't hard-fail when mold is not installed.
#
# To manually enable mold for all cargo invocations (not just make):
#   export RUSTFLAGS="-C link-arg=-fuse-ld=mold"
