## 1. Characterize the defect before changing the planner

- [x] 1.1 Add a failing unit test in `crates/lushtext-core/src/services/markdown_render.rs` proving a document shaped like the observed Helm README (heading, prose, one values table over `MARKDOWN_EVENTS_PER_PROJECTION_SLICE`, then several more headings and paragraphs) currently loses every event after the table.
- [x] 1.2 Add unit tests for the other real triggers — a bullet list over ~60 items and a blockquote with many paragraphs — asserting the same tail loss today. Note for later sections: the pinned `pulldown_cmark` 0.13.4 coalesces a **fenced** body into one `Text` event, so a fence is three events regardless of line count and its only pre-fix trigger was a body over the 256 KiB byte budget. An **indented** block emits one `Text` event per line, so it can reach the 256-event budget and is genuinely sub-sliceable.
- [x] 1.3 Add a unit test proving today's byte-budget path (`MARKDOWN_BYTES_PER_PROJECTION_SLICE`) also drops the tail, so both budgets are covered before the fix.
- [x] 1.4 Add a failing unit test for a 60-item **loose** list whose item 17's paragraph alone exceeds the event slice budget, asserting today's whole-document loss and pinning the intended post-fix outcome (items 1-16 and 18-60 rendered, item 17's paragraph replaced inside an otherwise-empty item 17). A loose item is required: a tight item is divisible at every bare inline-leaf boundary, so it cannot be made indivisible with plain text.
- [x] 1.5 Pin today's broken footnote numbering with the characterization test `test_footnote_numbering_restarts_across_projection_batches_today`. It asserts the *current* restart behavior rather than failing, because the widget harness has no `#[ignore]` support (`build.rs` scans for literal `#[test]`); task 7.7 inverts its assertions once section 5 lands.
- [x] 1.6 Record the baseline generated/killed mutant counts for `services/markdown_render.rs` in `evidence/mutation-baseline-markdown-render.md`, together with the exact reproduction command, so the post-change comparison is like-for-like. The baseline used scoped `scripts/run-mutants.sh full` (`MUTANTS_RE='markdown_render'` plus `MUTANTS_EXCLUDE_RE='delete field (active|pending) from struct'` to drop name-matched leakage from other modules), **not** `make mutants-diff` — the two generate different mutant sets.

## 2. GTK-free checkpoint policy in the planner

- [x] 2.1 Replace `plan_markdown_inner`'s `depth: usize` counter with an explicit open-frame stack whose length is still bounded by `MAX_MARKDOWN_STRUCTURE_DEPTH`, preserving today's depth metrics and the existing structural-depth terminal exactly.
- [x] 2.2 Classify frames with a wildcard-free exhaustive `match` over every `pulldown_cmark::Tag` variant (23 on the pinned 0.13 series) so a version bump that adds a tag is a compile error rather than a silent "cuttable" default. Block containers: `List`, `Item`, `Table`, `BlockQuote`, `CodeBlock`, `DefinitionList`, `DefinitionListDefinition`. Cut-forbidding: `Paragraph`, `Heading`, `TableHead`, `TableRow`, `TableCell`, `FootnoteDefinition`, `Emphasis`, `Strong`, `Strikethrough`, `Link`, `Image`, `Superscript`, `Subscript`, `DefinitionListTitle`, `HtmlBlock`, `MetadataBlock`. `FootnoteDefinition` is cut-forbidding by decision (see design Decision 1): it is structurally safe but has no taxonomy row, segment unit, or spec scenario, so it takes the top-level omission path instead.
- [x] 2.3 Add a unit test asserting `TableHead` is cut-forbidding (no cut between header cells, because `pulldown_cmark` emits header cells directly inside `TableHead`) while a cut after `End(TableHead)` is admissible.
- [x] 2.4 Implement the admissibility predicate: a batch boundary is admissible only when every open frame is a block container. Prove depth zero remains a special case of it and that ordinary documents produce byte-identical batch packing to today.
- [x] 2.5 Add the named ceilings beside the existing budget constants: `MAX_MARKDOWN_CARRIED_EMBED_BYTES = 64 * 1024` for **code-block text only**, a planner-side table cell ceiling mirroring `ui`'s `MAX_PREVIEW_TABLE_CELLS` (1,000) because `services` cannot import `ui`, and `MAX_MARKDOWN_PLACEHOLDER_WIDGETS = 64`. Each ceiling's comment states the widget budget it mirrors and that the pair must change together, that it bounds *retention only* (crossing carries counts forward, task 3.4), and — for the code ceiling — that it sits below the 256 KiB slice byte budget. A byte ceiling must not be applied to tables: an 8-cell table holding 192 KiB renders fully today, and a byte crossing would drop its rows.
- [x] 2.6 Implement sub-slicing: when accumulating a unit would exceed the per-slice event or byte budget and an admissible checkpoint exists at or before the current position, close the current batch there and continue the same block in the next batch. Blocks that fit a slice keep today's packing path untouched.
- [x] 2.7 Extend the existing per-batch budget assertions (`ordinary_blocks_are_packed_without_splitting`) to cover sub-sliced batches, so no over-budget batch can ever be emitted.

## 3. Omit the smallest overflowing unit and always continue

- [x] 3.1 Remove `MarkdownPlanLimit::TopLevelBlock` and `MarkdownPlanLimit::ProjectionBytes`, and add `MarkdownBlockOmission { reason, scope, unretained }` where `reason` distinguishes slice events, slice bytes, `CarriedEmbedBytes` (code-track byte ceiling), and `CarriedEmbedCells` (table-track cell ceiling) — two ceilings get two honest reasons rather than inferring the track from `unretained.cells > 0` — `scope` is `TopLevelBlock` or `ContainerSegment`, and `unretained` carries the source byte count and cell count of counted-but-unretained content (zero for the slice-budget reasons). Own the user-facing marker copy beside it.
- [x] 3.2 Delete the `break` on the oversized path: drop the overflowing unit's events, emit one omission marker at that position, count it in `MarkdownPlanMetrics`, and continue the event loop.
- [x] 3.3 Emit a `ContainerSegment` omission *inside the still-open container* so the container's sibling units (other rows, items, paragraphs, definitions) are unaffected, and reserve `TopLevelBlock` for a block with no interior checkpoint.
- [x] 3.4 Implement the embed ceilings as crossing-point behavior with no lookahead, one charge unit per track: retained text bytes for an open code block against `MAX_MARKDOWN_CARRIED_EMBED_BYTES`, and cell count for an open table against the mirrored cell ceiling. When appending an event would cross the open container's ceiling, stop retaining that container's remaining content, consume its remaining events for metrics only, and emit one in-container omission whose `unretained` payload holds the true remaining counts (bytes for code; cells plus their bytes for a table). Never mutate or truncate an already-emitted batch.
- [x] 3.5 Add a planner unit test pinning the `unretained` counts per track: a code block crossing the byte ceiling and a table crossing the cell ceiling each emit counts equal to the full observed remainder, so a projector charging them reproduces today's widget-budget decision exactly. Include the neutrality case — a table over 64 KiB but within the cell ceiling emits no omission and retains every cell.
- [x] 3.6 Expose the omission count on `MarkdownRenderPlan` (and keep `limit: Option<MarkdownPlanLimit>` meaning "a global budget stopped planning") so complete-with-omissions and stopped are separable without inspecting batches.
- [x] 3.7 Keep charging an omitted unit's events, retained bytes, and embed descriptors to `MarkdownPlanMetrics` and the global ceilings exactly as today. Pinned by two tests, split because the retained-byte terminal is not reachable end to end under the 4 MiB source cap: `omitted_units_still_charge_the_global_budgets` proves an all-indivisible document still reaches the global **events** terminal instead of bypassing it, and `retention_charge_arithmetic_is_pinned_for_a_retention_heavy_document` pins the retained-byte charge arithmetic directly (it asserts `limit == None`, because the terminal itself is unreachable; it does not assert the terminal fires).
- [x] 3.8 Cap `TopLevelBlock` fallback-widget markers at `MAX_MARKDOWN_PLACEHOLDER_WIDGETS`, render further top-level omissions as inline text markers, and render every `ContainerSegment` omission as an in-container text marker so container-segment omissions never add widgets.
- [x] 3.9 Confirm omission markers do not consume `MAX_MARKDOWN_EMBED_DESCRIPTORS` and add a unit test pinning that.
- [x] 3.10 Update the section 1 characterization tests to assert the fixed behavior: full tail present, correct omission scope and count, siblings preserved, and every global budget still terminal.

## 4. Batch continuation seam

- [x] 4.1 Add `MarkdownCarrySignature` (an ordered, depth-bounded list of `MarkdownOpenContainer` descriptors carrying only alignments, code language/kind, list ordinal, blockquote kind, and definition state) as the seam value object between planner and projector.
- [x] 4.2 Give `MarkdownEventBatch` the signature it expects at its first event and the signature it leaves open at its last, constructed at the single point where a batch is closed.
- [x] 4.3 Add planner unit tests for the chaining invariant: consecutive batches' signatures match, the final batch leaves nothing open, and signature length never exceeds `MAX_MARKDOWN_STRUCTURE_DEPTH`. Include a case where a carried-embed crossing ends a block mid-way, proving earlier batches for that block remain unchanged and still chain.
- [x] 4.4 Keep `plan_markdown`, `plan_markdown_cancellable`, `source_limited_markdown_plan`, and the 64-event cancellation cadence unchanged in contract; add a test that cancellation inside a sub-sliced block still retains no partial plan.

## 5. Generation-owned GTK projection continuation

- [x] 5.1 Add two sibling modules under `crates/lushtext-core/src/ui/markdown_preview/`: `continuation.rs` owning the generation-carried continuation type and batch application, and `text_flow.rs` holding stateless text-flow primitives over borrowed slices. The two-file split is a vertical role split forced by the file-size rule, not extra scope. Move `render_event_batch`'s block-level locals into it (`tag_stack`, `generic_blockquote_depth`, `list_stack`, `list_item_stack`, `definition_stack`, `pending_list_prefix`, `needs_block_separator`, footnote numbering, in-flight table and code-block buffers). Keep `mod.rs` from growing; it is already 2,541 lines.
- [x] 5.2 Validate the held continuation against each batch's expected signature before applying it; on mismatch publish an explicit terminal state and stop, never partially corrupted content. Compare container **identities** (kind and descriptor per frame, in order), not just frame counts — that is strictly stronger than the planner-side `assert_events_balanced` and is what catches a mis-chained carry that happens to have the right depth.
- [x] 5.3 Assert in debug builds that the inline-only locals (`active_text_links`, `active_image`) are empty at every batch boundary, and degrade to the explicit terminal in release rather than rendering split inline state.
- [x] 5.4 Keep an oversized table's rows appending into one `BufferedTableBuilder` and an oversized code block's lines into one `ActiveCodeBlock` (owning its `BufferedCodeBlock` and captured `EmbeddedBlockLayout`) across turns, so exactly one widget is inserted per source block. This is not only the large-embed case: because a checkpoint is admissible right after `Start(CodeBlock)`, a cut can land between a **tiny** code block's start and its `Text` event whenever the *enclosing* block overflows, so the single `ActiveCodeBlock` must survive a turn boundary that carries no content at all. Pinned by a deterministic 256-event-aligned fixture that lands the cut exactly on that boundary.
- [x] 5.5 On a crossed-embed omission, charge its `unretained` counts onto the in-flight buffer (`BufferedCodeBlock::source_bytes`, table cell count) before the block is finished, so `exceeds_preview_widget_budget()` and the table-cell check evaluate the block's true total and the existing `MAX_PREVIEW_TABLE_CELLS` / `MAX_PREVIEW_CODE_BLOCK_BYTES` fallback paths still trigger exactly as today.
- [x] 5.6 Make footnote numbering generation-scoped instead of batch-scoped and keep it bounded by the existing inline-footnote expansion budget.
- [x] 5.7 Drop or retire the continuation with the projection on generation change, cancellation, and widget teardown, routing an in-flight embed buffer through the existing guarded retirement/plain-disposal path so document-sized text is not freed on the GTK thread.
- [x] 5.8 Render markers for **user-visible omissions only**: skip the `CarriedEmbedBytes` and `CarriedEmbedCells` markers entirely (they are charge-carriers whose block is already replaced in place by the pre-existing fallback widget — see design Decision 4), while still charging their `unretained` counts per task 5.5. For the slice reasons, project scope-aware: `TopLevelBlock` via `build_preview_limit_fallback_widget` (inline text past the widget cap), and `ContainerSegment` as an in-container text marker — a spanning table row, an in-item paragraph, a quoted paragraph, or a definition body — with its own accessible description. The unit kind comes from the **enclosing container in the carry signature at the marker's position**, not from the omission value (which carries only `reason`, `scope`, `unretained`); fall back to the plan-side generic wording when the signature is empty.
- [x] 5.9 Remove the interim `INTERIM_OMISSION_TERMINAL` seam in `ui/markdown_preview/mod.rs` (marked `// Section 5 replaces this:`), which currently maps complete-with-omissions onto today's `Limited` terminal; task 6.1 supplies the real terminal.

## 6. Terminal state and user-facing copy

- [x] 6.1 Add a planner-side `MarkdownRenderPlan::user_visible_omissions()` accessor counting only the slice reasons, then add `MarkdownRenderState::Simplified` as a non-pending terminal for a complete projection whose `user_visible_omissions() > 0`. A plan whose only omissions are `CarriedEmbed*` publishes **`Complete`**. Keep `Limited` for global-budget stops and leaving `render_pending()` and the `preview-animation` readiness blocker semantics unchanged. This replaces the interim `INTERIM_OMISSION_TERMINAL` mapping removed in task 5.9; confirm no `Limited` path remains for an omission-only plan.
- [x] 6.2 Write the `Simplified` terminal copy as a completion statement whose count is `user_visible_omissions()` (never the raw marker total, which includes charge-carriers), and leave every remaining `MarkdownPlanLimit::description()` string byte-identical. String inventory: **two** old strings are already retired, not one — the interim seam maps both slice reasons onto the old `TopLevelBlock` copy, so the old `ProjectionBytes` wording ("one block exceeds the projection byte budget") has already disappeared from user-visible output. Account for both when auditing copy and updating widget-test assertions. Marker copy is separate from terminal copy and must be audited across **all four** reasons: the implemented `marker_text` is budget-naming and KiB-style, scope-aware, and carries no unit descriptor (task 5.8 supplies the unit name from the carry signature), so both `CarriedEmbedBytes` and `CarriedEmbedCells` need their own reviewed string.
- [x] 6.3 Set the preview surface's accessible description from the terminal state so complete, complete-with-omissions, stopped, failed, and cancelled remain distinguishable, and announce the omission count once rather than per marker.
- [x] 6.4 Confirm no automation snapshot field, readiness predicate, action, or AT-SPI anchor changes; if any does, update `docs/automation.md` and `docs/automation-reference.md` and run `make check-automation-docs`.

## 7. Tests and state extremes

- [x] 7.1 Extend the `markdown_render.rs` unit tests: `one_dense_block_uses_an_explicit_limited_terminal` and `one_large_text_block_uses_projection_byte_terminal` become omission-plus-continue cases; add sub-slicing cases for table rows, list items, code-block text runs, blockquote paragraphs, definition bodies, and a table nested in a list item.
- [x] 7.2 Add planner tests for the state extremes: empty document, single tiny block, one oversized block only, oversized block first / middle / last, several oversized blocks, one overflowing unit inside an otherwise normal container, container whose every unit overflows, oversized block immediately followed by a global-budget stop, and an oversized block inside a nested structure at the depth ceiling.
- [x] 7.3 Rewrite `crates/lushtext/tests/widget/markdown_preview.rs::test_dense_single_block_uses_accessible_limited_terminal` in place for the new `Simplified` terminal, its copy, and the accessible description, keeping the `AccessibleAudit` assertions.
- [x] 7.4 Add widget tests proving completeness with fixtures sized inside the window between the slice budget and the widget budgets: a 5-column by 100-row table (~500 cells, well over 256 events, under `MAX_PREVIEW_TABLE_CELLS`) rendering as one table widget with all rows; a 100-item ordered list with continuous numbering; a many-paragraph blockquote with correct rail depth; a many-entry definition list. Assert the document tail is present in `buffer_text()` for each. Code blocks split by kind: do **not** write a sub-sliced *fenced* fixture, since a fence is three events under the pinned parser, so a within-budget fence (for example ~16 KiB) is a *whole-render in one slice* assertion. Do add an *indented* fixture of over 256 lines but under 64 KiB, which genuinely sub-slices at text-run boundaries and must render as one continuous surface with every line in order. Leave the over-budget cases to task 7.5.
- [x] 7.5 Add widget tests for the degrade paths that stay in place, each crossing its own track's ceiling so they prove the counts-carrying mechanism: a code block over `MAX_PREVIEW_CODE_BLOCK_BYTES` and a table over `MAX_PREVIEW_TABLE_CELLS` each still produce exactly today's single fallback widget with the full byte/cell count in its text, **and publish `Complete` with zero rendered markers** — not `Simplified` — because a `CarriedEmbed*` omission is a charge-carrier, not a user-visible omission. Then the outcome-neutrality fixtures, which the two-track rule restores: a ≤1,000-cell table larger than 64 KiB (for example 900 cells of ~100 bytes) and a wide-cell table (for example 8 cells holding ~192 KiB) each still render every row with no fallback and no omission marker. The surrounding document renders in every case.
- [x] 7.6 Add a widget test for a single overflowing unit inside a container (one 400-inline-event paragraph in a **loose** list item among 60 items; one row with a huge cell in a normal table) asserting the sibling units render, the overflowing item still renders as an item with the marker inside it, and exactly one in-container marker appears with no extra widget.
- [x] 7.7 Invert the three assertions in task 1.5's characterization test `test_footnote_numbering_restarts_across_projection_batches_today` so it asserts continuous multi-batch reference/definition numbering, and rename it accordingly (there is no `#[ignore]` to remove). Prove continuation state under the adaptive shell with a real `LushtextWindow`-level test that drives `preview_layout_view` / `preview_split_view` transitions against an oversized-table fixture, keeping the existing bare-window primitive alongside it. Use the shared `present_window` / `wait_until` helpers from `crates/lushtext/tests/widget/common.rs` with async-scale budgets.
- [x] 7.8 Add widget tests for generation churn during a sub-sliced block: rerender mid-block, cancel mid-block, and teardown mid-block, asserting no stale widget or continuation survives and retirement counters stay bounded.
- [x] 7.9 Assert per-turn boundedness with `projection_counters_for_test()`: high-water events per turn stay within `MARKDOWN_EVENTS_PER_PROJECTION_SLICE` for every new oversized fixture.
- [x] 7.10 Add property coverage in the `property-tests` lane for the three plan invariants — every emitted batch is within both slice budgets, batch signatures chain, and — scoped to `limit.is_none()` plans only — every parsed event is projected or counted as omitted. The scoping is required: a zero-batch terminal plan is legal, since a global stop taken with an unclosed top-level block yields no batches at all.
- [x] 7.11 Fuzz surface settled and seeded: the `markdown_preprocess` target reaches planning through `exercise_markdown_for_fuzzing`, planning the **footnote-lowered** input so the fuzzed path matches the app's real path rather than raw source. `MARKDOWN_FUZZ_MAX_INPUT_LEN` is raised to 128 KiB, because the previous 4 KiB byte-ingestion bound cannot represent a >64 KiB fence and so could never reach the code-track crossing. Seeds added for seeds for the oversized-block and overflowing-unit shapes — wide tables, long lists, loose items with dense paragraphs, deep blockquotes, a table past the cell ceiling, a fence whose single coalesced body crosses the code byte ceiling, and an indented block of over 256 lines that sub-slices at text-run boundaries — and confirm planning stays deterministic and panic-free.

## 8. Documentation

- [x] 8.1 Update `docs/workflow-readability-matrix.md` WFR-MARKDOWN-PREVIEW census without declaring roles; the row stays `deferred`. Count **both** new files (`continuation.rs` ~1,026 lines, `text_flow.rs` ~265) and `mod.rs`'s reduction to ~1,955, and list the seam value objects `MarkdownCarrySignature`, `MarkdownOpenContainer`, `MarkdownBlockOmission`, and `MarkdownProjectionTerminal`.
- [x] 8.2 Update `docs/accessibility-matrix.md` A11Y-MARKDOWN-REPRESENTATIVE for omission markers (both scopes) and the complete-with-omissions terminal, and add the new widget/accessibility proof references. The row must state the marker-affordance distinction honestly: the **table-row** marker is a real accessible object with its own label, while the in-container markers for list items, quoted paragraphs, definition bodies, and code lines are plain buffer text with no AT-SPI anchor of their own — they are reachable through the preview's text interface, not as named objects.
- [x] 8.3 Update `docs/accessibility.md` for the marker announcement and the terminal-state distinction, carrying the same anchored-vs-buffer-text distinction as 8.2.
- [x] 8.4 Update `README.md` where it describes Markdown preview limits so it no longer implies an oversized block stops the preview.
- [x] 8.5 Update `docs/benchmarks/bounded-interactive-pipelines.md` where it states projection "applies at most 256 complete-block events" per turn; batches are no longer necessarily whole blocks.
- [x] 8.6 Update the `services/markdown_render.rs` module doc comment: the invariant is now "a batch boundary occurs only where no inline state is open", not "only at top-level depth zero". `render_event_batch` no longer exists, so the matching projector-side wording lives on `MarkdownProjectionContinuation::apply_batch`'s doc comment and the `continuation.rs` module header; update those instead.
- [x] 8.7 Update `docs/end-user-coverage.md` only if a smoke lane's expectations change.

## 9. Proof artifacts required by policy gates

- [x] 9.1 Run `make visual-geometry-smoke` from a clean `build/smoke/visual-geometry` root after the section 8 doc edits, so `check-visual-proof-policy` has a passing, unfiltered summary matching the current visual-sensitive diff (`crates/lushtext-core/src/ui/**` and `crates/lushtext/tests/widget/**` are visual-sensitive).
- [x] 9.2 Run `make visual-smoke` so `check-accessibility-policy` finds a `build/smoke/visual/summary.json` whose source fingerprint matches the tree.
- [x] 9.3 Run `make accessibility-smoke` and confirm the preview anchors and terminal descriptions still pass. Scope the "Marker replaces one unit inside a rendered container" proof to what actually exists: assert the **anchored table-row marker** through AT-SPI, and assert the remaining in-container markers as preview **buffer text** via the text interface. Do not add a new AT-SPI anchor for the plain-text markers in this change — anchoring buffer-text markers would mean giving each one its own accessible object, which is a presentation change beyond this change's scope; record that scoping decision beside the smoke manifest entry.
- [x] 9.4 Treat any later edit to a relevant file (including `docs/accessibility.md`, `docs/accessibility-matrix.md`, or any `ui/**` source) as voiding these fingerprints; rerun 9.1-9.3 before claiming the gates pass.

## 10. Verification

- [x] 10.1 Run `cargo fmt --all --check` and `git diff --check`.
- [x] 10.2 Run `make check` and fix every blocker in this work stream.
- [x] 10.3 Run `make check-policy` (includes `check-workflow-boundaries`, `check-accessibility-policy`, `check-visual-proof-policy`, `check-automation-docs`) and `make lint-advisory`.
- [x] 10.4 Run `make test-unit`, `make test-int`, and `make test-prop`.
- [x] 10.5 Run `make test-widget` through `scripts/run-widget-tests.sh --headless`, and rerun the new markdown preview tests in isolation; treat any `FLAKY:` line as a blocker per `.agents/rules/preexisting-blockers.md`.
- [x] 10.6 Run `make fuzz-corpus-replay` and confirm the new seeds replay clean without corpus mutation. Also check replay wall time: the largest committed seed is now ~75 KB (the raised `MARKDOWN_FUZZ_MAX_INPUT_LEN` makes large seeds representable), so confirm the lane stays fast enough to remain an ordinary PR job.
- [x] 10.7 Re-run the **exact** command recorded in `evidence/mutation-baseline-markdown-render.md` (scoped `scripts/run-mutants.sh full` with the same `MUTANTS_RE` / `MUTANTS_EXCLUDE_RE`), not `make mutants-diff`, and compare generated/killed counts for `services/markdown_render.rs` against the 1.6 baseline; a different command yields a different mutant set and is not parity evidence. Kill new survivors with deterministic tests rather than exclusions. Parity is shown on `services/markdown_render.rs` only: `ui/markdown_preview/**` is outside the mutation scope by design, because a deferred workflow gets no `policy.rs` and GTK adapters are not policy modules — that is not a coverage gap for 10.11 to flag.
- [x] 10.8 Run `make performance-smoke` and record planning/projection counters for the oversized-table and oversized-code fixtures without adding an absolute timing gate.
- [ ] 10.9 Run `make run`, open a real Helm-style README with an oversized values table, and confirm the full document renders, the table is one continuous widget, and stderr carries no GTK/GLib/pixman warning. Watch specifically for zero-width `Gtk::Grid::attach` criticals from sub-sliced table rows: the fix lands with the projector work, but only a live run confirms it, since a zero-width attach is exactly the class of defect a partially applied table can produce.

      **BLOCKED — not run.** A user-owned installed Flatpak instance of
      `dev.cominotti.lushtext` was running for the whole session (`flatpak ps`:
      instance `59484467`, pid `1479462`, runtime `org.gnome.Platform`; session-bus
      name `dev.cominotti.lushtext` owned via `xdg-dbus-proxy` pid `1479495`). It
      was not started by this work stream. `make run` force-quits the existing
      owner before staging its dev desktop entry, so running it would have killed
      the user's live editor session. The check is prepared and one command away: a
      representative Helm chart README fixture (40-row `| Key | Default |
      Description |` values table with realistic cell text, prose sections before
      and after, and a `PAGE-TAIL-MARKER:` line last) is written to the session
      scratchpad at `helm-values-README.md`. Headless equivalents of the same shape
      do pass: widget task 7.4's 5x100 (500-cell) table renders as one grid with
      every row, and the 10.8 performance evidence plans a 300x3 values table to
      completion in 14 batches with zero omissions and a 254-event per-slice high
      water. What remains unproven is only the live-session stderr scan for
      zero-width `Gtk::Grid::attach` criticals.
- [x] 10.10 Run `openspec validate continue-markdown-preview-past-oversized-blocks --strict` and `openspec validate --all --strict --no-interactive`.
- [x] 10.11 Review the final diff against the responsiveness, data-safety, hexagonal-boundary, GTK lifecycle, and comment-quality contracts; confirm no new dependency, action, D-Bus member, persisted format, evidence surface, or `policy.rs` entered the deferred markdown preview workflow.
